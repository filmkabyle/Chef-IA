[build]
  # Functions are now in netlify/functions (default location)
  # The function exports a path config at /api/generate

[functions]
  # Use the default functions directory netlify/functions
  directory = "netlify/functions"
